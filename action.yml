name: "Repo Owl"
description: "AI-powered repository assistant for pull request security analysis and automated documentation generation."
author: "Mohit Davar"

branding:
  icon: "shield"
  color: "blue"

inputs:
  github-token:
    description: "GitHub token used to read and write repository data."
    required: true

  openai-api-key:
    description: "OpenAI API key used for security analysis and documentation generation."
    required: true

  gitbook-token:
    description: "GitBook API token used to update GitBook documentation."
    required: false

  readme-api-key:
    description: "ReadMe API key used to update ReadMe documentation."
    required: false

  confluence-base-url:
    description: "Base URL of the Confluence instance"
    required: false
  confluence-username:
    description: "Confluence username or email address."
    required: false
  confluence-api-token:
    description: "Confluence API token used for authentication."
    required: false

runs:
  using: "node24"
  main: "dist/index.js"
